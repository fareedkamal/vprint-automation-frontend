# Frontend + API

This app is the **dashboard** for **vprint-automation** (Node API + FireSprint poller).

- **Env:** copy `.env.local.example` → `.env.local` and set `NEXT_PUBLIC_APP_URL` to your running API (see comments in the example file).
- **Full walkthrough** (ports, CORS, deploy):  
  [vprint-automation/docs/frontend-and-dashboard.md](https://github.com/fareedkamal/vprint-automation-service/blob/main/docs/frontend-and-dashboard.md)
